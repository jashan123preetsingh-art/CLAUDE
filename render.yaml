services:
  - type: web
    name: stockpulse
    runtime: node
    buildCommand: npm run render-build
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
